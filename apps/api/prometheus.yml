global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "newsiq-api"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["user-api:8000", "processing-api:8001"]
